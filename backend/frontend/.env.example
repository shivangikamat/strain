# Optional: set for local production-like builds (`npm run build && npm run preview`).
# In dev, leave unset so `/api` is proxied by Vite (see vite.config.ts).
# VITE_API_BASE_URL=https://strain-backend.onrender.com
